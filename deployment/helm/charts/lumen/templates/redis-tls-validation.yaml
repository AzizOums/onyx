{{- if and (eq (include "lumen.redisTls.enabled" .) "true") .Values.redis.enabled }}
{{- fail "redisTls.enabled requires redis.enabled=false because the bundled Redis does not support TLS" }}
{{- end }}
