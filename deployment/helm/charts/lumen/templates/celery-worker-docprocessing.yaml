{{- if and .Values.vectorDB.enabled (gt (int .Values.celery_worker_docprocessing.replicaCount) 0) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "lumen.resourceName" (list . "celery-worker-docprocessing") }}
  labels:
    {{- include "lumen.labels" . | nindent 4 }}
    {{- with .Values.celery_worker_docprocessing.deploymentLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if not .Values.celery_worker_docprocessing.autoscaling.enabled }}
  replicas: {{ .Values.celery_worker_docprocessing.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "lumen.selectorLabels" . | nindent 6 }}
      {{- if .Values.celery_worker_docprocessing.deploymentLabels }}
      {{- toYaml .Values.celery_worker_docprocessing.deploymentLabels | nindent 6 }}
      {{- end }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      {{- with .Values.celery_worker_docprocessing.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "lumen.labels" . | nindent 8 }}
        {{- with .Values.celery_worker_docprocessing.deploymentLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.celery_worker_docprocessing.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "lumen.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.celery_shared.podSecurityContext | nindent 8 }}
      {{- with .Values.celery_worker_docprocessing.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.celery_worker_docprocessing.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.celery_worker_docprocessing.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: celery-worker-docprocessing
          securityContext:
            {{- toYaml .Values.celery_shared.securityContext | nindent 12 }}
          image: "{{ .Values.celery_shared.image.repository }}:{{ .Values.celery_shared.image.tag | default .Values.global.version }}"
          imagePullPolicy: {{ .Values.global.pullPolicy }}
          command:
            [
              {{- with include "lumen.customCACerts.commandPrefix" . }}
              {{- . | nindent 14 }}
              {{- end }}
              "celery",
              "-A",
              "lumen.background.celery.versioned_apps.docprocessing",
              "worker",
              "--pool=threads",
              "--concurrency=6",
              "--prefetch-multiplier=1",
              {{- with .Values.celery_worker_docprocessing.logLevel }}
              {{ printf "--loglevel=%s" . | quote }},
              {{- end }}
              "--hostname=docprocessing@%n",
              "-Q",
              "docprocessing,port",
            ]
          ports:
            - name: metrics
              containerPort: 9093
              protocol: TCP
          resources:
            {{- toYaml .Values.celery_worker_docprocessing.resources | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ .Values.config.envConfigMapName }}
            {{- with .Values.extraEnvFromSecret }}
            - secretRef:
                name: {{ . }}
            {{- end }}
          env:
            - name: ENABLE_MULTIPASS_INDEXING
              value: "{{ .Values.celery_worker_docprocessing.enableMiniChunk }}"
            {{- include "lumen.envSecrets" . | nindent 12}}
            {{- with include "lumen.customCACerts.env" . }}
            {{- . | nindent 12 }}
            {{- end }}
            {{- with .Values.celery_worker_docprocessing.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with include "lumen.volumeMountsWithCA" (dict "ctx" . "volumeMounts" .Values.celery_worker_docprocessing.volumeMounts) }}
          {{- . | nindent 10 }}
          {{- end }}
          startupProbe:
            {{ .Values.celery_shared.startupProbe | toYaml | nindent 12}}
          readinessProbe:
            {{ .Values.celery_shared.readinessProbe | toYaml | nindent 12}}
            exec:
              command:
                - /bin/bash
                - -c
                - >
                    python lumen/background/celery/celery_k8s_probe.py
                    --probe readiness
                    --filename /tmp/lumen_k8s_docprocessing_readiness.txt
          livenessProbe:
            {{ .Values.celery_shared.livenessProbe | toYaml | nindent 12}}
            exec:
              command:
                - /bin/bash
                - -c
                - >
                    python lumen/background/celery/celery_k8s_probe.py
                    --probe liveness
                    --filename /tmp/lumen_k8s_docprocessing_liveness.txt
      {{- with include "lumen.volumesWithCA" (dict "ctx" . "volumes" .Values.celery_worker_docprocessing.volumes) }}
      {{- . | nindent 6 }}
      {{- end }}
{{- end }}
