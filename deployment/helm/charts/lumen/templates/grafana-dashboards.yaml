{{- if .Values.monitoring.grafana.dashboards.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lumen.resourceName" (list . "indexing-pipeline-dashboard") }}
  labels:
    {{- include "lumen.labels" . | nindent 4 }}
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Lumen"
data:
  lumen-indexing-pipeline.json: |
    {{- .Files.Get "dashboards/indexing-pipeline.json" | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lumen.resourceName" (list . "opensearch-search-latency-dashboard") }}
  labels:
    {{- include "lumen.labels" . | nindent 4 }}
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Lumen"
data:
  lumen-opensearch-search-latency.json: |
    {{- .Files.Get "dashboards/opensearch-search-latency.json" | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lumen.resourceName" (list . "redis-queues-dashboard") }}
  labels:
    {{- include "lumen.labels" . | nindent 4 }}
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Lumen"
data:
  lumen-redis-queues.json: |
    {{- .Files.Get "dashboards/redis-queues.json" | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lumen.resourceName" (list . "indexing-pruning-dashboard") }}
  labels:
    {{- include "lumen.labels" . | nindent 4 }}
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Lumen"
data:
  lumen-indexing-pruning.json: |
    {{- .Files.Get "dashboards/indexing-pruning.json" | nindent 4 }}
{{- end }}
