{{- if and .Values.vectorDB.enabled (gt (int .Values.celery_worker_scheduled_tasks.replicaCount) 0) (eq (toString (index .Values.configMap "ENABLE_CRAFT" | default "")) "true") }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "lumen.resourceName" (list . "celery-worker-scheduled-tasks") }}
  labels:
    {{- include "lumen.labels" . | nindent 4 }}
    {{- with .Values.celery_worker_scheduled_tasks.deploymentLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if not .Values.celery_worker_scheduled_tasks.autoscaling.enabled }}
  replicas: {{ .Values.celery_worker_scheduled_tasks.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "lumen.selectorLabels" . | nindent 6 }}
      {{- if .Values.celery_worker_scheduled_tasks.deploymentLabels }}
      {{- toYaml .Values.celery_worker_scheduled_tasks.deploymentLabels | nindent 6 }}
      {{- end }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      {{- with .Values.celery_worker_scheduled_tasks.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "lumen.labels" . | nindent 8 }}
        {{- with .Values.celery_worker_scheduled_tasks.deploymentLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.celery_worker_scheduled_tasks.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "lumen.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.celery_shared.podSecurityContext | nindent 8 }}
      {{- with .Values.celery_worker_scheduled_tasks.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.celery_worker_scheduled_tasks.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.celery_worker_scheduled_tasks.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: celery-worker-scheduled-tasks
          securityContext:
            {{- toYaml .Values.celery_shared.securityContext | nindent 12 }}
          image: "{{ .Values.celery_shared.image.repository }}:{{ .Values.celery_shared.image.tag | default .Values.global.version }}"
          imagePullPolicy: {{ .Values.global.pullPolicy }}
          command:
            [
              {{- with include "lumen.customCACerts.commandPrefix" . }}
              {{- . | nindent 14 }}
              {{- end }}
              "celery",
              "-A",
              "lumen.background.celery.versioned_apps.scheduled_tasks",
              "worker",
              {{- with .Values.celery_worker_scheduled_tasks.logLevel }}
              {{ printf "--loglevel=%s" . | quote }},
              {{- end }}
              "--hostname=scheduled_tasks@%n",
              "-Q",
              "scheduled_tasks",
            ]
          ports:
            - name: metrics
              containerPort: 9098
              protocol: TCP
          resources:
            {{- toYaml .Values.celery_worker_scheduled_tasks.resources | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ .Values.config.envConfigMapName }}
            {{- with .Values.extraEnvFromSecret }}
            - secretRef:
                name: {{ . }}
            {{- end }}
          env:
            {{- include "lumen.envSecrets" . | nindent 12}}
            {{- include "lumen.envSecretsRestricted" . | nindent 12}}
            {{- with include "lumen.customCACerts.env" . }}
            {{- . | nindent 12 }}
            {{- end }}
            {{- with .Values.celery_worker_scheduled_tasks.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with include "lumen.volumeMountsWithCA" (dict "ctx" . "volumeMounts" .Values.celery_worker_scheduled_tasks.volumeMounts) }}
          {{- . | nindent 10 }}
          {{- end }}
          startupProbe:
            {{ .Values.celery_shared.startupProbe | toYaml | nindent 12}}
          readinessProbe:
            {{ .Values.celery_shared.readinessProbe | toYaml | nindent 12}}
            exec:
              command:
                - /bin/bash
                - -c
                - >
                    python lumen/background/celery/celery_k8s_probe.py
                    --probe readiness
                    --filename /tmp/lumen_k8s_scheduledtasks_readiness.txt
          livenessProbe:
            {{ .Values.celery_shared.livenessProbe | toYaml | nindent 12}}
            exec:
              command:
                - /bin/bash
                - -c
                - >
                    python lumen/background/celery/celery_k8s_probe.py
                    --probe liveness
                    --filename /tmp/lumen_k8s_scheduledtasks_liveness.txt
      {{- with include "lumen.volumesWithCA" (dict "ctx" . "volumes" .Values.celery_worker_scheduled_tasks.volumes) }}
      {{- . | nindent 6 }}
      {{- end }}
{{- end }}
