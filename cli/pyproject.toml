[build-system]
requires = ["hatchling==1.29.0", "go-bin~=1.26.5", "manygo==0.2.0"]
build-backend = "hatchling.build"

[project]
name = "lumen-cli"
readme = "README.md"
description = "Terminal interface for chatting with your Lumen agent"
authors = [{ name = "Lumen AI", email = "founders@lumen.app" }]
requires-python = ">=3.9"
keywords = [
  "lumen", "cli", "chat", "ai", "enterprise-search",
]
classifiers = [
    "Programming Language :: Go",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
]
dynamic = ["version"]

[project.urls]
Repository = "https://github.com/lumen-dot-app/lumen"

[tool.hatch.build]
include = ["go.mod", "go.sum", "main.go", "**/*.go", "**/*.py", "README.md"]

[tool.hatch.version]
source = "code"
path = "internal/_version.py"

[tool.hatch.build.targets.wheel.hooks.custom]
path = "hatch_build.py"
binary_name = "lumen-cli"
tag_prefix = "cli"

[tool.uv]
managed = false
