[workspace]
resolver = "2"
members = ["lumen_text", "lumen-mcp-server", "lumen-db"]

[workspace.package]
edition = "2021"
license = "MIT"
publish = false

# Shared pins so the two crates never disagree on a common dependency.
[workspace.dependencies]
anyhow = "1.0"
axum = "0.8"
chrono = { version = "0.4", default-features = false, features = ["serde", "std", "clock"] }
prometheus = { version = "0.14", default-features = false }
regex = "1.13"
# 0.13.2+ is required by rmcp's client transport, used by the integration tests.
reqwest = { version = "0.13.2", default-features = false, features = [
    "json",
    # Pulls rustls-platform-verifier, which honours the system trust store and
    # therefore the custom CA bundle the Helm chart mounts.
    "rustls",
    "http2",
] }
rmcp = { version = "3.3", default-features = false, features = [
    "server",
    "macros",
    "transport-streamable-http-server",
] }
serde = { version = "1.0", features = ["derive"] }
uuid = { version = "1", features = ["serde", "v4"] }
serde_json = "1.0"
thiserror = "2.0"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal", "sync", "time"] }
tower-http = { version = "0.7", features = ["cors"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
